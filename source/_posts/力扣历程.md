---
title: 力扣历程
date: 2026-09-21 20:09:11
tags: [数据结构,算法,C++,Java]
---

这篇笔记用于记录在力扣刷题的历程、想法等，不过也会有其他平台的题。

## 2026.9

### 两数之和

给定一个整数数组`nums`和一个整数目标值`target`，请你在该数组中找出 和为目标值`target`的那 两个 整数，并返回它们的数组下标。

你可以假设每种输入只会对应一个答案，并且你不能使用两次相同的元素。

你可以按任意顺序返回答案。

{% tabs 两数之和, 1 %}
<!-- tab C++ -->
```cpp
#include <vector>
#include <unordered_map>

using namespace std;

class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        // 创建哈希表，key为数组元素的值，value为元素的下标
        unordered_map<int, int> num_map;
        
        for (int i = 0; i < nums.size(); ++i) {
            int complement = target - nums[i];
            
            // 在哈希表中查找补数是否存在
            auto it = num_map.find(complement);
            if (it != num_map.end()) {
                // 找到则返回两个下标
                return {it->second, i};
            }
            
            // 未找到则将当前元素及下标存入哈希表
            num_map[nums[i]] = i;
        }
        
        return {}; // 题目保证有解，此处仅为语法要求
    }
};
```
<!-- endtab -->

<!-- tab Java -->
```java
import java.util.HashMap;
import java.util.Map;

class Solution {
    public int[] twoSum(int[] nums, int target) {
        // 创建 HashMap，key为数组元素的值，value为元素的下标
        Map<Integer, Integer> map = new HashMap<>();
        
        for (int i = 0; i < nums.length; i++) {
            int complement = target - nums[i];
            
            // 判断 HashMap 中是否包含目标差值
            if (map.containsKey(complement)) {
                // 找到则返回两个下标
                return new int[] { map.get(complement), i };
            }
            
            // 未找到则将当前元素及下标存入 HashMap
            map.put(nums[i], i);
        }
        
        // 题目保证有解，实际不会走到这里
        throw new IllegalArgumentException("No two sum solution");
    }
}
```
<!-- endtab -->
{% endtab